{
  "name": "babel-beats",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/snapshot-sg.mjs --if-stale",
    "dev": "vite",
    "prebuild": "node scripts/snapshot-sg.mjs --if-stale",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "catalog": "node scripts/build-catalog.mjs",
    "snapshot:sg": "node scripts/snapshot-sg.mjs",
    "playlists": "node scripts/build-playlists.mjs"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "opencc-js": "^1.4.2",
    "pinyin-pro": "^3.29.4",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
